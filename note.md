# MYDB代码阅读笔记
## tm/TransactionManager
    [XIDCounter(8字节)][TransactionStatus(1字节)]...[TransactionStatus(1字节)]
    1.这是一个基于文件的事务状态管理器，核心功能是管理事务的生命周期状态，确保事务的原子性和持久性。
    2这个事务管理器通过一个.xid文件来持久化记录所有事务的状态信息。
    3.文件开头8字节存储当前最大事务ID，之后每个事务占用1字节记录其状态（活跃/提交/回滚）。
    4.当开始新事务时，系统会分配一个比当前最大ID大1的事务ID，在文件中对应位置标记为活跃状态，并更新文件头的最大ID计数器。
    提交或回滚事务时，只需修改对应位置的状态值并强制刷盘确保持久化。
    系统启动时会验证文件完整性，确保事务状态记录与文件头信息一致，从而为上层数据库提供可靠的事务状态管理服务，保证事务的原子性和持久性特性。
## dm/loger 日志
### 1.日志记录格式设计
    日志文件整体结构：[XChecksum（4字节）] [Log1] [Log2] ... [LogN] [BadTail]
                     XChecksum：后续所有日志的校验和  Log1...LogN：正常的日志记录  BadTail：可能存在的损坏尾部数据
    单条日志结构：[Size(4字节)] [Checksum(4字节)] [Data(变长)]
                Size：数据部分的长度   Checksum：数据部分的校验和 Data：实际的日志数据
    使用校验和机制保证单条日志和整个文件的完整性
### 2.日志写入流程
    接收数据后，通过 wrapLog()方法包装成完整日志格式
    使用文件锁保证多线程安全写入
    采用追加写入模式，定位到文件末尾后写入新日志
    写入后立即更新全局校验和 xChecksum 并强制刷盘
### 3.日志读取机制
    通过 next()方法顺序读取日志记录
    内部使用 internNext()实现具体的读取逻辑
    读取时验证单条日志的校验和，确保数据完整性
    自动跳过损坏的日志记录，返回纯数据部分给调用者
### 4. 崩溃恢复处理
    系统启动时自动调用 checkAndRemoveTail()方法
    重新计算文件中所有完整日志的累积校验和
    与文件头存储的 xChecksum 对比验证文件完整性
    发现不一致时自动截断损坏的尾部数据
### 5. 文件管理功能
     提供 create()和 open()静态工厂方法创建/打开日志文件
    支持基于偏移量的 truncate()操作清理损坏数据
    rewind()方法重置读取位置到日志数据开始处
    close()方法确保资源正确释放
## dm/DataItem
    [ValidFlag(1字节)][DataSize(2字节)][Data(变长)]
    ValidFlag: 标识数据项是否有效（0合法，1非法/已删除）
    DataSize: 记录实际数据的长度
    Data: 实际的数据内容
### 1. 核心功能模块
    数据包装：wrapDataItemRaw()将原始数据包装成标准格式
    数据解析：parseDataItem()从页面指定位置解析数据项
    有效性管理：提供有效性检查和设置方法
    数据访问：通过data()方法获取数据部分
### 3. 事务支持机制
    Before-After模式：实现原子性操作
    before()：修改前备份数据、获取锁、标记脏页
    after()：修改后记录日志、释放锁
    unBefore()：事务回滚时恢复数据
    日志记录：通过DataManager记录重做日志
    脏页标记：确保修改的页面能被正确刷回磁盘
### 4. 并发控制实现
    读写锁机制：
    读锁(rLock/rUnLock)：支持并发读取
    写锁(lock/unlock)：保证排他性写入
    线程安全：所有数据访问都通过锁机制保护
    细粒度锁：每个数据项独立加锁，提高并发性能
### 5. 生命周期管理
    资源获取：通过DataManager读取数据项
    引用计数：release()方法管理资源释放
    缓存集成：与数据管理器的缓存机制协同工作
    内存管理：避免内存泄漏，确保资源及时回收
### 6. 页面集成设计
    页面关联：每个数据项关联到具体的Page对象
    偏移量定位：通过UID（页面号+偏移量）精确定位
    内存映射：使用SubArray避免不必要的数据拷贝
    脏页管理：修改时自动标记页面为脏页

### 7. 异常处理与恢复
    逻辑删除：通过ValidFlag支持软删除，便于恢复
    数据备份：修改前完整备份，支持事务回滚
    状态一致性：确保崩溃恢复后数据一致性
    资源清理：完善的资源释放机制
### 8. 性能优化特性
    零拷贝设计：SubArray直接操作页面数据，避免内存拷贝
    预分配内存：oldRaw缓冲区预分配，减少动态分配开销
    锁分离：读写锁分离，提高读并发性能
    局部性原理：相关数据集中存储，提高缓存命中率